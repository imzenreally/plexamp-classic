# plexamp-classic config — copy to .env (gitignored)
# PLEX_HOST = your Plex server URL, PLEX_TOKEN = your X-Plex-Token,
# PLEX_SECTION = your music section key (optional; first music section is used if omitted)
PLEX_HOST=http://your-plex-server:32400
PLEX_TOKEN=your-plex-token-here
PLEX_SECTION=